import { DashboardView } from "@/components/dashboard/DashboardView";

/**
 * Dashboard route.
 * TODO(UI): additional routes (markets, watchlist, portfolio, insights) are the
 * ROUTING agent's to define — this file is the entry screen only.
 */
export default function DashboardPage() {
  return <DashboardView />;
}
